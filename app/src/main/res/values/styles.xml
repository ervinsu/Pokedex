<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:textColorSecondary">@color/blackWhite</item>
        <item name="actionOverflowButtonStyle">@style/DotsDarkTheme</item>
        <item name="drawerArrowStyle">@style/DrawerToggleDarkTheme</item>
        <item name="android:textColorPrimary">@color/blackWhite</item>
        <item name="android:textColor">@color/blackWhite</item>
    </style>

    <style name="DotsDarkTheme" parent="@style/Widget.AppCompat.ActionButton.Overflow" >
        <item name="android:tint">@color/blackWhite</item> 
    </style>

    <style name="DrawerToggleDarkTheme" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/blackWhite</item>
    </style>

    <style name="ThemeOverlay.DotsDarkTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/whiteBlack</item>
        <item name="android:textColor">@color/blackWhite</item>
    </style>

    <style name="CustomProgressBarHorizontal" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:scaleY">1</item>
        <item name="android:max">150</item>
        <item name="android:minWidth">100sp</item>
        <item name="android:progressTint">@color/blackWhite</item>
        <item name="android:progressBackgroundTint">@color/detailLight</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
